{
  "name": "kp-upload-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for R2 Uploads",
  "main": "worker.js",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy"
  }
}
